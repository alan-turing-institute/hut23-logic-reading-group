% -*- TeX-engine: luatex; fill-column: 72; eval: (auto-fill-mode -1); eval: (visual-fill-column-mode 1); eval: (visual-line-mode 1); eval: (adaptive-wrap-prefix-mode 1) -*-
\documentclass{article}
\usepackage{ttn}
\usepackage{booktabs}
%\usepackage{mathtools}
\usepackage{colonequals} % because unicode-math has broken "::=" somehow
\usepackage{simplebnf}
%
\newcommand{\myrules}{
\ndrules % include standard rules
\renewcommand{\ii}{\by{$\to$I}}
\renewcommand{\ie}{\by{$\to$E}}
\def\x{\by{X}}
\def\ip{\by{IP}}}
\usepackage[rules=myrules]{fitch}
% \usepackage[raggedrightboxes]{ragged2e}
\usepackage[Ragged, size=footnote, shape=up, alerton]{sidenotesplus}
\usepackage{subcaption}
\DeclareCaptionStyle{sidecaption}{font=footnotesize, justification=raggedright}
\usepackage[dvipsnames]{xcolor}
%% 
%%\usepackage[style=authoryear]{biblatex}
%%\addbibresource{notes.bib}
%%
%%
%% \defn takes an optional star and an optional argument.
%% If the optional argument is present, it is used for the marginal note; if the argument is missing, the default is the mandatory argument. if the command is starred, no marginal note is made. 
\NewDocumentCommand\defn{sO{#3}m}{\textit{#3}\IfBooleanF{#1}{\sidenote*{\textcolor{MidnightBlue}{#2}}}}
%\newcommand{\defn}[2][#2]{\textit{#2}\sidenote*{#1}}
\newcommand{\isdef}{\stackrel{\text{def}}{=}}
% Relies on euler-math.sty, loaded by ttn.sty
\newcommand{\wff}[1]{\ensuremath{\symscr{#1}}}
\newcommand{\tr}{\ensuremath{\mathbb{t}}}
\newcommand{\fa}{\textcolor{.!50}{\ensuremath{\mathbb{f}}}}
%% 
\title{All we know about logic}
\author{James Geddes}
\date{\today}
%%
\begin{document}%\maketitle
\maketitle
\section{Preliminaries}

In the following, ``the book'' means \emph{forall x: Calgary}.

\subsection{Arguments in English}
\label{sec:english}
A \defn{sentence} is a statement that could in principle be true or
false.  It is not, for example, a question or an interjection.  (The
book says that ``a \textsc{sentence} [is] a sentence that can be true or
false'' which seems circular.  On the other hand, I have not defined a
``statement.'')

An \defn{argument} is a finite sequence of sentences.  The final sentence is called the \defn[conclusion, premises]{conclusion}; the other sentences are called the \defn*{premises}. 

A \defn{counterexample} to an argument is a situation in which all the
premises are true but the conclusion is false.  An argument is
\defn[valid / invalid]{valid} if it has no counterexamples.  Thus, an
argument is valid if, whenever the premises are all true, then
necessarily the conclusion is true.  An argument that is not valid is
\defn*{invalid}.

The conclusion of a valid argument may be false if its premises are false.  A valid argument whose premises are true is \defn{sound}. If an argument is sound then, indeed, its conclusion may be judged to be true. 

If it is not possible for a sentence to be false, it is \defn[necessary
truth / falsehood]{necessarily true}.  For example: ``The number 7 is
either odd or even.''  If it is not possible for a sentence to be true,
it is \defn*{necessarily false}.  A sentence that is neither necessarily
true nor necessarily false is called \defn{contingent}.

Two sentences are \defn{jointly equivalent} if in all cases either they
are both true or they are both false.  A set of sentences are
\defn[jointly possible / impossible]{jointly possible} if there is a
case in which they are all true; sentences that are not jointly possible
are \defn*{jointly impossible}.

\sidenote*{The book refers to the logic developed in parts II to~IV\ as
  \emph{truth-functional logic}, or TFL\@.  Truth-functional logic is
  also known as \emph{propositional logic} and sometimes
  \emph{sentential logic} (see Appendix~A.1 of the book).  The Stanford
  Encyclopedia of Philosophy calls it propositional logic, so that is
  what I shall do.}
\section{Propositional logic}

\subsection{Syntax}
We assume the existence of a countable set of \defn[sentence
letter]{sentence letters}: $A$, $B$, \ldots, $A_1$, $A_2$, \ldots,
$C_{42}$, \ldots.  The symbols $\wedge$, $\vee$, $\to$,
$\leftrightarrow$, and $\neg$ are known as \defn[connectives ($\wedge$,
$\vee$, $\to$, $\leftrightarrow$, and $\neg$)]{connectives}.  A \defn[sentence (or ``proposition'')]{sentence} (of
propositional logic) is defined by the grammar shown in
figure~\ref{fig:bnf-prop}.

\begin{figure}[ht]
  \sidecaption{\label{fig:bnf-prop}The meaning of ``sentence'' in
    propositional logic.  Here \wff{S}, \wff{A}, and \wff{B} are
    arbitrary sentences.  An alternative name for a sentence is a
    \emph{well-formed formula}. Sometimes the symbols $\top$ (``top,'' or
    ``truth'') and $\bot$ (``bottom,'' or ``false'') are also allowed as
    sentences.}
  \centering
  \begin{bnf}
    $\wff{S}$ : Sentence ::=
    | $A$ // $B$ // $C$ // $\dotsb$        : atomic sentence
    |  $\neg \wff{A}$                      : negation
    | ($\wff{A} \wedge \wff{B}$)           : conjuction 
    | ($\wff{A} \vee \wff{B}$)             : disjunction
    | ($\wff{A} \to \wff{B}$)              : conditional
    | ($\wff{A} \leftrightarrow \wff{B}$)  : biconditional
  \end{bnf}
\end{figure}

It is traditional, when writing a sentence, to omit the outer set of
parentheses and to allow the use of other kinds of brackets, such as `['
and `]', wherever parentheses may be used. It is also traditional (but
this is not a tradition followed by the book) to omit many other
parentheses when the result can be disambiguated according to the
following rules: \sidenote*{I tend to think of the \emph{real} thing as
  a kind of data type, or perhaps a tree.  The sentence is just how we
  serialise it.  That is, I understand, say, ``a conjunction'' to simply
  be a kind of sentence, the constituent data for which is a pair of
  sentences (namely, the left conjunct and the right conjunct).  It
  doesn't matter how it is \emph{written}, what it \emph{is} is a
  conjunction.  The only reason we care about how we write it is to
  ensure that we can recover the real thing given the written form. I don't know whether this view is right.}
\begin{enumerate}
\item $\neg$ binds more tightly than $\wedge$ and $\vee$, which bind more tightly
  than $\to$ and $\leftrightarrow$;
\item $\wedge$ and $\vee$ are associative so it doesn't matter how you replace the parentheses in, for example, $P\wedge Q\wedge R$; and
\item $\to$ is taken to be right associative.
\end{enumerate}
Thus $P\wedge Q\to R$ means $(P\wedge Q)\to R$; $P\to Q\to R$ means $P \to (Q \to R)$; and $P\wedge Q \wedge R$ may be read as either $(P\wedge Q)\wedge R$ or $P\wedge (Q\wedge R)$, as you like.

\subsection{Object language and metalanguage}

The grammar of figure~\ref{fig:bnf-prop} defines the set of possible
sentences in propositional logic. However, the terms `\wff{S}',
`\wff{A}', and `\wff{B}', (written in a script typeface) are not
themselves either sentences or parts of sentences.  They cannot be, for
they are neither atomic propositions nor sentences made from atomic
propositions and connectives.  Instead they \emph{stand for} arbitrary sentences.  Whereas `$P$', `$Q$', and so on are propositional variables, which are truth-valued, the script variables, `\wff{A}', `\wff{B}', and so on are \defn{metavariables} and are ``sentence valued.'' 

Propositional variables, and sentences generally, make up the
\defn{object language} of propositional logic.  Metavariables and other
terms used to described the object language are said to be part of the
\defn{metalanguage}.
% The symbol `$\therefore$', used to indicate which sentence
% is the conclusion of an argument, is also part of the metalanguage.
% TODO: Maybe? Or should we not be using \vDash ?

\subsection{Semantics}

\subsubsection{Valuations}

A \defn[truth values ($\tr$ and $\fa$)]{truth value} is one of `$\tr$'
(true) or `$\fa$' (false).  (The lighter shade for $\fa$ is just to make
it clearer which is which.)  Sentences are the kind of thing that may
have truth values.  A \defn{valuation} is an assignment of a truth value
to each of a set of sentence letters.  A \defn{truth table} for a
sentence is a set of rows, each giving a possible valuation of the
sentence letters in the sentence together with the value of the whole
sentence.  (The truth value of a compound sentence is determined
entirely by the truth values of its constituent parts and the particular
connectives.)  See figure~\ref{fig:truthtables} for complete truth
tables of the standard connectives.
\begin{figure}[ht]
  \sidecaption{\label{fig:truthtables}Truth tables for the standard
    connectives. Note that that the column headers, `\wff{A}',
    `\wff{B}', and so on, are metavariables rather than sentence
    letters. If `$\top$' and `$\bot$' are permitted as sentences, their valuations are always $\tr$ and $\fa$ respectively.}
  \centering 
  \begin{tabular}{@{}c|c@{}}
    $\wff{A}$ & $\neg \wff{A}$ \\
    \specialrule{\cmidrulewidth}{0pt}{0pt} 
    \fa & \tr \\
    \tr & \fa \\
  \end{tabular}\medskip
  
  \begin{tabular}{@{}cc|c@{}}
    $\wff{A}$ & $\wff{B}$ & $\wff{A} \wedge \wff{B}$ \\
    \specialrule{\cmidrulewidth}{0pt}{0pt} 
    \fa & \fa & \fa \\
    \fa & \tr & \fa \\
    \tr & \fa & \fa \\
    \tr & \tr & \tr \\
  \end{tabular}\hfil
  \begin{tabular}{@{}cc|c@{}}
    $\wff{A}$ & $\wff{B}$ & $\wff{A} \vee \wff{B}$ \\
    \specialrule{\cmidrulewidth}{0pt}{0pt} 
    \fa & \fa & \fa \\
    \fa & \tr & \tr \\
    \tr & \fa & \tr \\
    \tr & \tr & \tr \\
  \end{tabular}\hfil
  \begin{tabular}{@{}cc|c@{}}
    $\wff{A}$ & $\wff{B}$ & $\wff{A} \to \wff{B}$ \\
    \specialrule{\cmidrulewidth}{0pt}{0pt} 
    \fa & \fa & \tr \\
    \fa & \tr & \tr \\
    \tr & \fa & \fa \\
    \tr & \tr & \tr \\
  \end{tabular}\medskip
  
  \begin{tabular}{@{}cc|c@{}}
    $\wff{A}$ & $\wff{B}$ & $\wff{A} \leftrightarrow \wff{B}$ \\
    \specialrule{\cmidrulewidth}{0pt}{0pt} 
    \fa & \fa & \tr \\
    \fa & \tr & \fa \\
    \tr & \fa & \fa \\
    \tr & \tr & \tr \\
  \end{tabular}
\end{figure}

\subsubsection{Arguments (reprise)}

We now recapitulate the ideas of section~\ref{sec:english} as they apply to propositional logic rather than in general.

A sentence, \wff{S}, is a \defn[tautology, contradiction]{tautology} if
it is true on every valuation.  Likewise, \wff{S} is a
\defn*{contradiction} if it is false on every valuation.  ``Tautology''
and ``contradiction'' are the the propositional logic versions of
``necessary truth'' and ``necessary falsehood'' respectively.

Two sentences, \wff{A} and \wff{B}, are \defn{equivalent} if their truth values agree on every valuation.

Sentences $\wff{A}_1, \wff{A}_2, \dotsc, \wff{A}_n$ are \defn[jointly
satisfiable / unsatisfiable]{jointly satisfiable} if there is some
valuation that makes them all true.  Likewise these sentences are
\defn*{jointly unsatisfiable} if there is no valuation that makes them
all true.  These concepts are the propositional logic versions of
``jointly possible'' and ``jointly impossible'' respectively.

Sentences $\wff{A}_1, \wff{A}_2, \dotsc, \wff{A}_n$ \defn[entailment, $\vDash$]{entail} sentence $\wff{C}$ if there is no valuation that makes all the $\wff{A}_i$ true and $\wff{C}$ false.  If indeed the $\wff{A}_i$ entail $\wff{C}$ we write
\begin{equation*}
  \wff{A}_1, \wff{A}_2, \dotsc, \wff{A}_n \vDash \wff{C}.
\end{equation*}

Entailment is the propositional logic version of validity. If
$\wff{A}_1, \dotsc, \wff{A}_n \vDash \wff{C}$, then the argument
$\wff{A}_1, \dotsc, \wff{A}_n\, \therefore \wff{C}$ is valid.  The converse is
not necessarily true since in English, but not in propositional logic,
one may appeal to the meaning of the sentence letters to establish
validity.

If $\wff{C}$ is a tautology we may write $\vDash \wff{C}$. Likewise, if $\wff{C}$ is a contradiction we may write $\wff{C} \vDash$. If the sentences $\wff{A}_1,\dotsc, \wff{A}_n$ are jointly unsatisfiable, we may write $\wff{A}_1,\dotsc, \wff{A}_n \vDash$.  

\subsection{Natural deduction}

\subsubsection{Proofs}

A \defn{proof} is a numbered sequence of
\emph{lines}. Figure~\ref{fig:egproof} shows an example proof of seven
lines.
\begin{marginfigure}
  \caption{\label{fig:egproof}A proof of
    $(A \wedge B) \to C\; \therefore\, A \to (B \to C)$ in Fitch notation. On any line, the
    ``nesting depth'' is shown by the number of vertical lines to the
    left of the proposition.}
 \small \[
  \begin{nd}
    \hypo{hyp}{(A \wedge B) \to C} \by{PR}{}
    \open
     \hypo{A}{A}          \by{AS}{}
     \open
      \hypo{B}{B}         \by{AS}{}
      \have{4}{A \wedge B}     \ai{A, B}
      \have{5}{C}         \ie{hyp, 4}
     \close
     \have{6}{B \to C}      \ii{B-5}
    \close
    \have{7}{A \to (B \to C)} \ii{A-6}
  \end{nd}
\]
\end{marginfigure}%
Together with its number, each line comprises:
\begin{enumerate}
\item a propositional formula (that is, a sentence);
\item the rule by which this line was derived (to be explained later); 
\item a list of \emph{antecedents} (the numbers of the previous lines
  from which this line is obtained) or an indication that this line is a
  premise or an assumption); and 
\item a \emph{nesting depth}\sidenote{``Nesting depth'' is not a
    standard term.} (a natural number, also to be explained later).
\end{enumerate}
\emph{Example:} On line~\ndref{4} of the proof shown in figure~\ref{fig:egproof}, the propositional formula is $A \wedge B$, the derivation rule is $\wedge I$, the antecedents are lines \ndref{A} and~\ndref{B}, and the nesting depth is~3.

The rules of proofs are as follows:
\begin{enumerate}
\item The nesting depth starts at~1, indicated by a single vertical line
  after the line number. There is then a line for each premise (having
  the derivation rule ``PR'') followed by a horizontal rule.
\item Each subsequent line in the proof is then one of three types:
  \begin{enumerate}
  \item \emph{Derivation}. (No change of nesting depth.) An application
    of one of the derivation rules (see table~\ref{tab:derivrules}). The
    antecedents are those required by the derivation rule and must all
    be lines above this one (that is, having smaller line numbers)
    \emph{and} have a nesting depth no greater than this one (that is,
    one may not refer to lines in sub-proofs that have already
    concluded).

  \item \emph{Sub-proof introduction}. (Nesting depth increases by~1.)
    The increase in nesting depth is shown by an extra vertical line to
    the left. The proposition for this line maybe be any propositional
    formula and is the assumption of the sub-proof, indicated by ``AS''
    as the derivation rule.  (In some sense, the opening of the whole
    proof is simply an instance of this type of line, albeit with
    multiple assumptions.) One writes a horizontal rule below the
    assumption.
  \item \emph{Sub-proof conclusion}. (Nesting depth decreases by~1.)
    Certain derivation rules reduce the nesting depth and ``close out''
    a sub-proof. The antecendents are precisely the lines of the
    sub-proof.
  \end{enumerate}
\end{enumerate}

\subsubsection{Derivation rules}
\begin{margintable}
  \caption{Derivation rules for natural deduction proofs.\label{tab:derivrules}}\centering
  \begin{tabular}{@{}lr@{}}
    \toprule
    Rule & Figure  \\
    \midrule
    $\wedge$I and $\wedge E$ & \ref{fig:rule:conj} \\
    $\vee$I and $\vee E$ & \ref{fig:rule:disj} \\
    $\to$I and $\to E$ & \ref{fig:rule:impl} \\
    $\neg$I and $\neg E$ & \ref{fig:rule:neg} \\
    \midrule
    R (Reiteration) & \ref{fig:rule:reit}  \\
    X (Explosion)   & \ref{fig:rule:expl} \\
    \midrule
    IP (Indirect proof) & \ref{fig:rule:ind} \\
    \bottomrule
  \end{tabular}
\end{margintable}
For each connective, there is a pair of derivation rules. They are known
as ``introduction rules'' (where the connective is introduced by the
rule) and ``elimination rules'' (where the connective is removed by the
rule).
% Conjunction
\begin{figure}[ht]\centering
  \sidecaption{Conjunction. Given both $\wff{A}$ and $\wff{B}$ one may
    conclude $\wff{A\wedge B}$. Contrariwise, given $\wff{A} \wedge \wff{B}$ one may conclude $\wff{A}$ and
    also $\wff{B}$. \label{fig:rule:conj}}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Conjunction introduction.}
    \[
      \begin{nd}
        \have[m]{m}{\wff{A}}
        \have[n]{n}{\wff{B}}
        \have[~]{o}{\wff{A} \wedge \wff{B}} \ai{m, n}
      \end{nd}
    \]
  \end{subfigure}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Conjunction elimination.}
    \begin{equation*}
      \begin{nd}
        \have[m]{m}{\wff{A \wedge B}}
        \have[~]{o}{\wff{A}} \ae{m}
      \end{nd}
    \end{equation*}
    \begin{equation*}
      \begin{nd}
        \have[m]{m}{\wff{A \wedge B}}
        \have[~]{o}{\wff{B}} \ae{m}
      \end{nd}
    \end{equation*}
  \end{subfigure}
\end{figure}

%% Disjunction
\begin{figure}[ht]\centering
  \sidecaption{Disjunction. Given $\wff{A}$ (or given $\wff{B}$) one may conclude $\wff{A}\vee \wff{B}$. Contrariwise, given $\wff{A}\vee \wff{B}$, a proof of $\wff{C}$ from $\wff{A}$, and a proof of $\wff{C}$ from $\wff{B}$, one may conclude~$\wff{C}$.\label{fig:rule:disj}}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Disjunction introduction.}
    \[
      \begin{nd}
        \have[m]{m}{\wff{A}}
        \have[~]{o}{\wff{A} \vee \wff{B}} \oi{m}
      \end{nd}
    \]
    \[
      \begin{nd}
        \have[m]{m}{\wff{B}}
        \have[~]{o}{\wff{A} \vee \wff{B}} \oi{m}
      \end{nd}
    \]
    \end{subfigure}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Disjunction elimination.}
    \[
      \begin{nd}
        \have[m]{m}{\wff{A} \vee \wff{B}}
        \open
        \hypo[i]{i}{\wff{A}} \by{AS}{}
        \have[j]{j}{\wff{C}}
        \close
        \open
        \hypo[k]{k}{\wff{B}} \by{AS}{}
        \have[l]{l}{\wff{C}}
        \close
        \have[n]{n}{\wff{C}} \oe{m,i-j,k-l}
      \end{nd}
    \]
  \end{subfigure}
\end{figure}

%% Conditional
\begin{figure}[ht]\centering
  \sidecaption{Conditional. If in the present context one may infer
    $\wff{B}$ from an assumption of~$\wff{A}$, then one may conclude $\wff{A}\to\wff{B}$. Conversely if one has $\wff{A}\to\wff{B}$ and also $\wff{A}$, then one may conclude~$\wff{B}$.\label{fig:rule:impl}}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Conditional introduction.}
    \[
      \begin{nd}
        \open
        \hypo[i]{i}{\wff{A}}  \by{AS}{}
        \have[j]{j}{\wff{B}}
        \close
        \have[~]{}{\wff{A} \to \wff{B}} \ii{i-j}
      \end{nd}
    \]
    \end{subfigure}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Conditional elimination (aka, \emph{modus ponens}).}
    \[
      \begin{nd}
        \have[m]{m}{\wff{A} \to \wff{B}}
        \have[n]{n}{\wff{A}}
        \have[~]{}{\wff{B}} \ie{m,n}
      \end{nd}
    \]
  \end{subfigure}
\end{figure}
The rules for negation follow the same pattern but note that now the
symbol ``$\bot$'' is required.\sidenote{And you might wonder what happened to $\top$.}
%% Negation
\begin{figure}[ht]\centering
  \sidecaption{Negation. If an assumption of $\wff{A}$ leads to a
    contradiction, then one may conclude~$\neg\wff{A}$. Conversely, if one
    has both $\neg\wff{A}$ and $\wff{A}$ then one may
    conclude~$\bot$.\label{fig:rule:neg}}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Negation introduction.}
    \[
      \begin{nd}
        \open
        \hypo[i]{i}{\wff{A}} 
        \have[j]{j}{\bot}
        \close
        \have[~]{}{\neg\wff{A}} \ni{i-j}
      \end{nd}
    \]
    \end{subfigure}
  \begin{subfigure}{0.48\textwidth}
    \subcaption{Negation elimination.}
    \[
      \begin{nd}
        \have[m]{m}{\neg\wff{A}}
        \have[n]{n}{\wff{A}}
        \have[~]{}{\bot} \ne{m,n}
      \end{nd}
    \]
  \end{subfigure}
\end{figure}

There are two rules that \emph{sort} of follow the pattern of
introduction and elimination if you squint. ``Reiteration'' is something
like ``from a known proposition one may conclude the same proposition,''
and ``explosion'' is ``from a contradiction one may conclude
anything.''
%% Reiteration
\begin{figure}[ht]
  \sidecaption{Reiteration and explosion. From $\wff{A}$ one may at any
    time derive~$\wff{A}$. However, from a contradiction anything at all follows.}
  \begin{subfigure}{0.48\textwidth}
  \subcaption{Reiteration.\label{fig:rule:reit}}
  \[
    \begin{nd}
      \have[m]{m}{\wff{A}}
      \have[~]{o}{\wff{A}}\r{m}
    \end{nd}
  \]
\end{subfigure}
\begin{subfigure}{0.48\textwidth}
  \subcaption{Explosion (aka, \textit{ex falso quodlibet}).\label{fig:rule:expl}}
  \[
    \begin{nd}
      \have[m]{m}{\bot}
      \have[~]{o}{\wff{A}} \x{m}
    \end{nd}
  \]
\end{subfigure}
\end{figure}

Finally, there is a lone rule called ``indirect
proof.''\sidenote{Indirect proof is sort of the ``fifth postulate'' of
  classical logic. It's not like the others, there are alternatives, and
  some people reject it.}
\begin{figure}[ht]
  \sidecaption{Indirect proof (aka, \emph{reductio ad absurdum}). To prove $\wff{A}$, start by assuming $\neg\wff{A}$ and derive a contradiction.\label{fig:rule:ind}}
  \[
    \begin{nd}
      \open
      \hypo[i]{na}{\neg\wff{A}}
      \have[~]{false}{\bot}
      \close
      \have[~]{a}{\wff{A}} \ip{i}
    \end{nd}
  \]
\end{figure}

\end{document}
